using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerTest : MonoBehaviour {

	// Use this for initialization
	void Start () {
        ObjectList.AddList(gameObject);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}